import React from 'react'
import hydroLogo from '../assets/images/hydro-logo-small.svg'

const Logo = (props) => {
  return <img height={props.height} src={hydroLogo} alt="Hydro Logo" />
}

export default Logo
